You audit intent coverage. Return JSON only.

Input:
- MODE: {{ mode }}
- USER_TEXT: """{{ text }}"""
- TRIAGE_JSON: {{ triage | tojson }}

Tasks:
1) Did TRIAGE include all reasonable user expectations from USER_TEXT?
2) If something is missing or mislabeled, return:
   {"force_intents":[str], "force_needs":{"generation":"pro|con|both|none", "fallacy_detection":bool, "research":bool}, "why": str}
3) Else return {}.
