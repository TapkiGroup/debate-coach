fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.8
python-dotenv>=1.0
openai>=1.40
pytest>=8.0
pytest-asyncio>=0.23   # if you add async tests; safe to include
anyio>=4.4             # for @pytest.mark.anyio style tests
ruff>=0.5              # linter/formatter
black>=24.8            # formatter (optional if you rely on ruff format)
mypy>=1.10             # type checking (optional)